import { ComingSoon } from '@/components/layout/ComingSoon';
export default function InterviewPage() {
  return <ComingSoon title="Interview Simulator" subtitle="45-min timer + random problems + AI evaluation." day={6} />;
}