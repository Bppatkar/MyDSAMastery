import { ComingSoon } from '@/components/layout/ComingSoon';
export default function DashboardPage() {
  return <ComingSoon title="Progress Dashboard" subtitle="Streaks, heatmaps, pattern analytics." day={6} />;
}