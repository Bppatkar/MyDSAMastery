import { ComingSoon } from '@/components/layout/ComingSoon';
export default function VisualizersPage() {
  return <ComingSoon title="Algorithm Visualizers" subtitle="Sorting, Graphs, Trees, DP — all animated." day={4} />;
}