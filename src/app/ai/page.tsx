import { ComingSoon } from '@/components/layout/ComingSoon';
export default function AiPage() {
  return <ComingSoon title="AI Tutor" subtitle="Pattern detector + guided hints + DSA chat — coming soon." day={6} />;
}
