import { ComingSoon } from '@/components/layout/ComingSoon';
export default function QuestionPage() {
  return <ComingSoon title="Problem Solver" subtitle="Monaco editor + AI hints + test cases." day={3} />;
}